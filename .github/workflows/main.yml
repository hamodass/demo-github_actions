name: CICD first workflow

on:
    push:
      branches:
          - main

jobs:
   example-job:
     runs-on: ubuntu-latest


     steps:
       - name: Print a welcome message
         run: echo "Welecom to my first CICD workflow"

